id: xss-reflected
info:
  name: Reflected XSS
  author: project-c
  severity: high
requests:
  - method: GET
    path:
      - "{{BaseURL}}/search?q=<script>alert(1)</script>"
    matchers:
      - type: word
        words:
          - "<script>alert(1)</script>"
